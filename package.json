{
  "name": "webservice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "cover": "jest --coverage --coverageReporters=text-lcov | coveralls",
    "test2": "nyc --reporter=html --reporter=text mocha --timeout=3000 --exit",
    "test": "nyc --reporter=html --reporter=text mocha --timeout=3000 --exit | coveralls",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "coverage2": "coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vinils/WebService.git"
  },
  "author": "VLS",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/vinils/WebService/issues"
  },
  "homepage": "https://github.com/vinils/WebService#readme",
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "coveralls": "^3.0.6",
    "istanbul": "^0.4.5",
    "jest": "^24.9.0",
    "mocha": "^6.2.1",
    "mocha-lcov-reporter": "^1.3.0",
    "nodemon": "^1.19.3",
    "nyc": "^14.1.1"
  },
  "dependencies": {
    "body-parse": "^0.1.0",
    "express": "^4.17.1"
  },
  "engines": {
    "node": "12"
  }
}
